{
  "name": "lyrics2emoji",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "server": "node --env-file-if-exists=.env server/api.js",
    "dev": "npx serve public -l 8080"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.1098.0",
    "@aws-sdk/client-dynamodb": "^3.1098.0",
    "@aws-sdk/client-s3": "^3.1098.0",
    "@aws-sdk/lib-dynamodb": "^3.1098.0"
  }
}
